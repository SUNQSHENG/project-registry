# 个人数据 — 永不提交（隐私防线）
PROJECTS.json
backups/
*.bak

# 开发记录（面向 AI，含内部决策，不进入公开仓库；本地保留照常生效）
CLAUDE.md

# 系统/编辑器
.DS_Store
Thumbs.db

# 自动保存隐私（对话原文，绝不提交）
.memory/

# 决策归档（含内部决策细节，绝不提交）
.archive/

# 内部工作文件（tickets/设计文档，开发产物不发布）
.scratch/
docs/token-saving-plan.md

# 本地生成脚本（含个人路径，不发布）
docs/make_cards.py

# npm 安装器
installer/node_modules/
installer/*.tgz

# Python 缓存
__pycache__/
*.pyc
